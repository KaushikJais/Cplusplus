#include <iostream>
#include <cstring>
#include <sstream>
#include <math>
using namespace std;

bool CheckValidInvalid(int N){
	bool result = true;
	int x=0;
	int curElem =0;
	cin>>curElem;
	for(int i=1;i<N;++i){
		cin>>x;
		if()
	}
}

int main(){
	int N;
	cin>>N;
	bool ans = CheckValidInvalid(N);
	if(ans){
		cout<<"True";
	}
	else{
		cout<<"False";
	}

	return 0;
}